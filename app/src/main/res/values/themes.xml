<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Notes" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/day_primary</item>
        <item name="colorPrimaryVariant">@color/day_dark_primary</item>
        <item name="colorOnPrimary">@color/day_text</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/day_light_primary</item>
        <item name="colorSecondaryVariant">@color/day_primary</item>
        <item name="colorOnSecondary">@color/day_primary_text</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="itemBackground">@color/day_text</item>
    </style>

    <style name="Theme.Notes.Background">
        <item name="itemBackground">?attr/colorOnPrimary</item>
    </style>

    <style name="Theme.Notes.Buttons" parent="Widget.MaterialComponents.Button.TextButton">
        <item name="android:textColor">?attr/colorOnSecondary</item>
    </style>
</resources>